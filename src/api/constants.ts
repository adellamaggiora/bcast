import {environment} from "../environments/environment"

export const authKey = environment.authKey;
export const supabaseUrl = environment.supabaseUrl;
export const supabaseAnonKey = environment.supabaseAnonKey;